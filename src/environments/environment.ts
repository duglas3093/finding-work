// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

//llave de firebase
export const firebaseConfig = {
  apiKey: "AIzaSyCIrQYEcRR10DBUKnA6dzpncVoXEAvUpTs",
  authDomain: "finding-work.firebaseapp.com",
  databaseURL: "https://finding-work.firebaseio.com",
  projectId: "finding-work",
  storageBucket: "finding-work.appspot.com",
  messagingSenderId: "539947702087",
  appId: "1:539947702087:web:daf2db1744b728b8e51e62"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
